<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>se.deversity</groupId>
        <artifactId>blindbean-parent</artifactId>
        <version>0.1.0</version>
    </parent>

    <artifactId>blindbean-processor</artifactId>
    <name>BlindBean Annotation Processor</name>
    <description>The compile-time processor that turns a @BlindEntity class into its BlindWrapper.
        Depends only on blindbean-annotations — it generates code that references the runtime as
        text, so a consumer's compile path never pulls the runtime, the native bridge, or the
        Vector API.</description>
    <url>https://github.com/PIsberg/blindbean</url>
    <licenses><license><name>PolyForm Noncommercial License 1.0.0</name><url>https://polyformproject.org/licenses/noncommercial/1.0.0/</url></license></licenses>
    <developers><developer><id>PIsberg</id><name>Peter Isberg</name></developer></developers>
    <scm><url>https://github.com/PIsberg/blindbean</url></scm>

    <dependencies>
        <dependency><groupId>se.deversity</groupId><artifactId>blindbean-annotations</artifactId></dependency>
        <!-- AutoService generates META-INF/services so the processor is found on the CLASSPATH.
             The module-info `provides` covers the MODULE path. Both are needed. -->
        <dependency>
            <groupId>com.google.auto.service</groupId>
            <artifactId>auto-service</artifactId>
            <version>1.1.1</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>26</source>
                    <target>26</target>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                        <arg>--add-modules</arg>
                        <arg>jdk.incubator.vector</arg>
                        <!-- This module overrides compilerArgs, so it must repeat the reactor-root
                             pointer or its guardrails never reach the merged repo-root files. -->
                        <arg>-Avibetags.root=${maven.multiModuleProjectDirectory}</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>com.google.auto.service</groupId>
                            <artifactId>auto-service</artifactId>
                            <version>1.1.1</version>
                        </path>
                        <!--
                          This module overrides the parent's annotationProcessorPaths, so the
                          VibeTags RC5 generator must be listed here too or the processor's own
                          @AI* guardrails vanish from the merged root files. Versionless: managed
                          by the imported vibetags-bom. Emits blindbean-processor/.claude/rules/
                          (grouped by its .vibetags-roles) + its sidecar fragment for the root merge.
                        -->
                        <path>
                            <groupId>se.deversity.vibetags</groupId>
                            <artifactId>vibetags-processor</artifactId>
                            <!-- Pinned via the shared property (not versionless): an overriding
                                 annotationProcessorPaths does not reliably inherit the bom-managed
                                 version and Maven mis-resolved it to a sibling path's version. -->
                            <version>${vibetags.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
