# Copilot instructions — BlindBean

Project overview, build flags and architecture live in [CLAUDE.md](../CLAUDE.md).

The guardrail block below is **generated by VibeTags** from the `@AI*` annotations in the
Java sources — edit the annotations and recompile, never the generated markdown. Per-class
detail is emitted into `.github/instructions/` and applied by `applyTo:` globs, so this file
stays a lean index rather than a second copy.
