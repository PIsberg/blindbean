<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  BlindBean Checkstyle configuration — deliberately LEAN.

  This is a build-failing gate (mvn verify), so it targets real correctness / hygiene defects,
  not formatting or documentation nitpicks (no line-length, no Javadoc-required, no naming
  conventions beyond the obvious). The goal is a signal-dense gate that stays green as the
  library evolves without turning every whitespace change into a red build.
-->
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- suppress noise on module-info.java and any generated sources -->
    <module name="SuppressionFilter">
        <property name="file" value="${checkstyle.suppressions.file}"/>
    </module>

    <module name="TreeWalker">
        <!-- allow // CHECKSTYLE.OFF / ON escape hatches for the rare justified case -->
        <module name="SuppressionCommentFilter"/>

        <!-- imports: no wildcards in a modular library — keeps exports intentional -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports">
            <property name="processJavadoc" value="true"/>
        </module>

        <!-- blocks & control flow -->
        <module name="EmptyBlock">
            <property name="option" value="text"/>
            <property name="tokens" value="LITERAL_TRY, LITERAL_FINALLY, LITERAL_IF, LITERAL_ELSE, LITERAL_FOR, LITERAL_WHILE, LITERAL_DO, LITERAL_SWITCH"/>
        </module>
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignored"/>
        </module>
        <module name="FallThrough"/>
        <module name="DefaultComesLast"/>

        <!-- correctness smells -->
        <module name="EqualsHashCode"/>
        <module name="StringLiteralEquality"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="ModifiedControlVariable"/>
        <module name="OneTopLevelClass"/>
        <module name="UpperEll"/>
        <module name="ArrayTypeStyle"/>
    </module>
</module>
